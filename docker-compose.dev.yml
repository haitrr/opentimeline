services:

  postgres:
    image: postgres:17
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "54231:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: